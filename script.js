document.addEventListener('DOMContentLoaded', () => {
    const welcomeText = document.getElementById('welcome-text');
    welcomeText.style.animationPlayState = 'running'; // Starts the animation
});
